#pragma once

struct Color {
    int r; // Componente rojo (0 a 255)
    int g; // Componente verde (0 a 255)
    int b; // Componente azul (0 a 255)
    int a; // Componente alfa (transparencia) (0 a 255)
};
